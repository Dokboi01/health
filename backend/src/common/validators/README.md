# Shared Validators

This folder is reserved for reusable validation helpers that can be shared across modules.

Examples that belong here:

- phone number validators
- UUID helpers
- date range validators
- file upload metadata validators
- reusable pagination fragments

Module-specific request validation should still live in each module's `*.schemas.ts` file.

