name: careaxis_mobile
description: Premium medical health app for admins, doctors, and patients.
publish_to: none

version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  flutter_riverpod: ^3.0.3
  go_router: ^16.3.0
  google_fonts: ^8.0.2
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

