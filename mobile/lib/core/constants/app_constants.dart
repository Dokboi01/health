class AppConstants {
  static const String appName = 'CareAxis';
  static const String apiBaseUrl = 'http://10.0.2.2:4000/api/v1';
  static const bool enableFirebaseMessaging = true;

  // While the backend is being developed locally, the app can run in demo mode.
  static const bool useMockAuth = true;
}
