# Shared Repositories

This folder is reserved for repository abstractions that are shared across multiple features.

Examples:

- user session repository
- uploaded file repository
- shared settings repository

Feature-specific repositories should stay inside each feature's `data/` folder.

